= render 'shared/header'

#posts
  - if @posts.any?
    / - @posts.each do |p|
    /   .post-stub
    /     %em= p.published_at.strftime("%b %e %Y")
    /     %h3.title= link_to p.title, post_path(p.slug)
    = render @posts
  - else
    .post-stub
      %em Welcome! More content coming soon.

= will_paginate @posts

= render 'shared/footer'